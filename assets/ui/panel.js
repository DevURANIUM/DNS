(()=>{'use strict';
const root=document.documentElement;
const isCustomer=document.body.classList.contains('customer');
try{root.dataset.theme=isCustomer?(localStorage.getItem('dns-theme')||'dark'):'dark';}catch(_){root.dataset.theme='dark';}
const toast=document.createElement('div');toast.className='toast';toast.setAttribute('role','status');document.body.append(toast);let timer;
function notify(message){toast.textContent=message;clearTimeout(timer);timer=setTimeout(()=>{toast.textContent='';},3500);}
const theme=document.createElement('button');theme.type='button';theme.className='ghost theme-toggle';
function themeLabel(){theme.textContent=root.dataset.theme==='light'?'حالت تاریک ◐':'حالت روشن ◑';theme.setAttribute('aria-label',theme.textContent);}
themeLabel();theme.onclick=()=>{root.dataset.theme=root.dataset.theme==='light'?'dark':'light';try{localStorage.setItem('dns-theme',root.dataset.theme);}catch(_){}themeLabel();};
if(isCustomer)(document.querySelector('header')||document.body).append(theme);
document.querySelectorAll('nav a.on').forEach(a=>a.setAttribute('aria-current','page'));
document.querySelectorAll('body.customer input[type=password]').forEach((input,index)=>{
 if(!input.id)input.id='password-'+index;
 const label=input.previousElementSibling;if(label?.tagName==='LABEL'&&!label.htmlFor)label.htmlFor=input.id;
 const wrapper=document.createElement('div');wrapper.className='password-field';input.before(wrapper);wrapper.append(input);
 const button=document.createElement('button');button.type='button';button.className='ghost password-toggle';button.setAttribute('aria-controls',input.id);
 function render(){const show=input.type==='text';const title=show?'پنهان کردن رمز':'نمایش رمز';button.setAttribute('aria-label',title);button.title=title;button.setAttribute('aria-pressed',String(show));button.innerHTML='<svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M2 12s3.5-7 10-7 10 7 10 7-3.5 7-10 7S2 12 2 12Z"/><circle cx="12" cy="12" r="3"/>'+(show?'<path d="m3 3 18 18"/>':'')+'</svg>';}
 button.onclick=()=>{input.type=input.type==='password'?'text':'password';render();};render();wrapper.append(button);
});
document.querySelectorAll('label').forEach((label,index)=>{if(label.htmlFor||label.querySelector('input,select,textarea'))return;const field=label.nextElementSibling;if(field&&field.matches('input,select,textarea')){if(!field.id)field.id='field-'+index;label.htmlFor=field.id;}});
const normalize=value=>value.normalize('NFKC').replace(/ي/g,'ی').replace(/ك/g,'ک').replace(/[۰-۹]/g,c=>String('۰۱۲۳۴۵۶۷۸۹'.indexOf(c))).replace(/[٠-٩]/g,c=>String('٠١٢٣٤٥٦٧٨٩'.indexOf(c))).toLocaleLowerCase();
const serviceCards=Array.from(document.querySelectorAll('details.svc'));
if(serviceCards.length){const search=document.createElement('input');search.type='search';search.className='service-search';search.placeholder='جست‌وجوی سرویس یا دامنه…';search.setAttribute('aria-label','جست‌وجوی سرویس یا دامنه');const count=document.createElement('p');count.className='muted';count.setAttribute('aria-live','polite');serviceCards[0].before(search,count);function filterServices(){const q=normalize(search.value.trim());let n=0;serviceCards.forEach(card=>{card.hidden=!normalize(card.textContent).includes(q);if(!card.hidden)n++;});count.textContent=n+' از '+serviceCards.length+' گروه';}search.addEventListener('input',filterServices);filterServices();}
document.querySelectorAll('table').forEach((table,index)=>{
 const wrapper=document.createElement('div');wrapper.className='table-scroll';wrapper.tabIndex=0;wrapper.setAttribute('role','region');wrapper.setAttribute('aria-label','جدول؛ برای دیدن ستون‌ها پیمایش کنید');table.before(wrapper);wrapper.append(table);
 const rows=Array.from(table.rows).filter(row=>row.querySelector('td'));if(rows.length<2)return;
 const toolbar=document.createElement('div');toolbar.className='toolbar';const search=document.createElement('input');search.type='search';search.placeholder='جست‌وجو در این جدول…';search.setAttribute('aria-label','جست‌وجو در جدول');search.id='table-search-'+index;
 const count=document.createElement('output');count.setAttribute('aria-live','polite');const reset=document.createElement('button');reset.type='button';reset.className='ghost';reset.textContent='پاک کردن';toolbar.append(search,reset,count);wrapper.before(toolbar);
 const empty=document.createElement('p');empty.className='empty-result';empty.textContent='نتیجه‌ای پیدا نشد. عبارت جست‌وجو را تغییر دهید.';empty.hidden=true;wrapper.after(empty);
 const headers=Array.from(table.rows[0].cells).map(cell=>cell.textContent.trim());const statusIndex=headers.indexOf('وضعیت');let statusFilter;
 if(statusIndex>=0){statusFilter=document.createElement('select');statusFilter.setAttribute('aria-label','فیلتر وضعیت');const all=document.createElement('option');all.value='';all.textContent='همه وضعیت‌ها';statusFilter.append(all);new Set(rows.map(row=>row.cells[statusIndex].textContent.trim())).forEach(status=>{const option=document.createElement('option');option.value=status;option.textContent=status;statusFilter.append(option);});toolbar.insertBefore(statusFilter,reset);statusFilter.addEventListener('change',filter);}
 rows.forEach(row=>Array.from(row.cells).forEach((cell,i)=>cell.querySelectorAll('input:not([type=hidden]),select').forEach(field=>{if(!field.getAttribute('aria-label'))field.setAttribute('aria-label',headers[i]+' — '+row.cells[0].textContent.trim());})));
 function filter(){const query=normalize(search.value.trim());let visible=0;rows.forEach(row=>{const value=row.textContent+' '+Array.from(row.querySelectorAll('input:not([type=hidden]),select')).map(el=>el.tagName==='SELECT'?el.selectedOptions[0]?.textContent||'':el.value).join(' ');row.hidden=!normalize(value).includes(query)||Boolean(statusFilter?.value&&row.cells[statusIndex].textContent.trim()!==statusFilter.value);if(!row.hidden)visible++;});count.textContent=visible+' از '+rows.length+' مورد';empty.hidden=visible>0;}
 search.addEventListener('input',filter);table.addEventListener('change',filter);reset.onclick=()=>{search.value='';if(statusFilter)statusFilter.value='';filter();search.focus();};filter();
});
document.querySelectorAll('.dns .big').forEach(address=>{const button=document.createElement('button');button.type='button';button.className='ghost copy-dns';button.setAttribute('aria-label','کپی آدرس DNS');button.title='کپی آدرس DNS';button.innerHTML='<svg width=18 height=18 viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.8" aria-hidden="true"><rect x=8 y=8 width=12 height=12 rx=3></rect><path d="M15 8V5a2 2 0 0 0-2-2H5a2 2 0 0 0-2 2v8a2 2 0 0 0 2 2h3"></path></svg>';const line=document.createElement('div');line.className='dns-address';address.before(line);line.append(address,button);button.onclick=async()=>{try{await navigator.clipboard.writeText(address.textContent.trim());notify('آدرس DNS کپی شد');}catch(_){const range=document.createRange();range.selectNodeContents(address);const selection=window.getSelection();selection.removeAllRanges();selection.addRange(range);notify('آدرس انتخاب شد؛ آن را دستی کپی کنید');}};
 const guide=document.createElement('details');guide.className='guide';const summary=document.createElement('summary');summary.textContent='راهنمای اتصال و رفع مشکل';guide.append(summary);['۱. ابتدا با اینترنت دستگاه موردنظر وارد پنل شوید و آی‌پی آن را ثبت کنید.','۲. آدرس بالا را در بخش DNS دستی تنظیمات شبکه وارد کنید؛ در صورت نیاز، برای DNS دوم نیز همین آدرس را بنویسید.','۳. اتصال شبکه را دوباره برقرار کنید. اگر سرویس کار نکرد، وضعیت پلن و سهمیه را بررسی کنید.','۴. پس از تغییر اینترنت یا راه‌اندازی مجدد مودم، آی‌پی را دوباره در پنل ثبت کنید.'].forEach(text=>{const p=document.createElement('p');p.textContent=text;guide.append(p);});address.closest('.dns').append(guide);
});
})();
